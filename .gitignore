.screenshots/
node_modules/
.DS_Store
Thumbs.db
*.local
.env
.playwright-report/
test-results/
# Mesures partielles de tools/compare-routes.mjs --only (jamais le rapport complet)
docs/COMPARAISON-53-ROUTES.partiel.md
# Relevés partiels de tools/composants.mjs (une route, une largeur, une surcharge de diagnostic) :
# ils n'écrasent plus le relevé complet, et n'ont pas à être versionnés.
docs/composants.partiel.json
docs/GROUPES-ECARTS.partiel.md
# Sondes de diagnostic jetables, écrites au cours d'une passe et sans valeur hors de celle-ci.
.tfp-diag/
# Relevé de baseline en cours d'écriture : le fichier est réécrit après CHAQUE contrôle, il ne
# peut donc pas rester propre tant que le relevé tourne. Le versionner une fois terminé, en le
# forçant (`git add -f`).
docs/baseline-2026-*.json

# Archives d’export : elles se refabriquent depuis export/html-brut et export/site-navigable.
export/*.zip
# Rapports Lighthouse complets (JSON + HTML), reproduits par tools/lighthouse.mjs.
export/lighthouse/

# Dossier de validation G28 : régénéré à la demande depuis le banc, jamais commité.
# Les captures pèsent des dizaines de Mo et seraient périmées au premier correctif ;
# ce sont les ARCHIVES de release/ qui constituent la livraison.
docs/dossier-g28/
